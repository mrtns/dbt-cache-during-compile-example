cacheduringcompile:
    target: dev
    outputs:
        dev:
            type: postgres
            threads: 1
            host: localhost
            port: 5432
            user: postgres
            pass: mysecretpassword
            dbname: postgres
            schema: cacheduringcompile
